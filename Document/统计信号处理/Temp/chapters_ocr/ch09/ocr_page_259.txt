244
第一卷：统计信号处理基础
一估计理论
T;的均值为
N-1
μ = E(Ti)
E(α[n]) =E(r[n])
0=1
利用例9.2的结果，T，的方差为
N-1
var(s[n]]
var(T)
var
n=0
但是
var(r[n])
α²[n]^exp(-^[n]) dr[n] -
12
var(T) =
N)?
由于 (T)=1/T 
89
8T,
根据（9.15）式可求得近似的均值为
E() = g(μ1) =
根据（9.16）式可求得近似的方差为
var()
var(T))
8TIT=μ
可以看出，估计量是近似无偏的，并且近似的方差随着N的增大而减小。需要强调
的是,这些表达方式只是近似，它和9的线性化精度是一致的。实际上在本例中，可以
很容易证明是MLE。这样，它的渐近PDF(当N→8时）可以证明是（参见(7.8)式）
XN(),X/N)
因此，只有当N-→时近似值是精确的。（在寸题9.10中,利用9的二次展开来推导的
均值和方差的二阶近似。）为了确定均值和方差表达式成立所要求的N值究竞要多大，必
须进行蒙特卡洛计算机模拟。
利用泰勒级数方法的基本前提是函数9在p(T;6)必须为非零的T的范围内是近似线性
的。在下面情况中，这是很自然的。
1.数据记录足够大，以至于如前面例子中所描述的，PT;β)集中在均值附近。在例子中，当
N→0时,由于 var( T,)=1(N/>")0,所以当 N→0时, T, = ≥*=α[n］ 的 PDF 变得
更加集中在F（T)附近。