146
第一卷：统计信号处理基础一估计理论
许范围在区间［α，中，那么我们只需在此区问上便（x；)最大即可。此时，最“安全"的方
法在[&，区间采用如图7.8所示的网格搜索法。对于给定的数据集，只要值的间隔足够
小，我们就保证可求出其MLE。，当然，对于一个新的数据集，因为似然函数肯定会改变，所以
将不得不重复使用网格搜紧法。然而，如果的范围没有控制在有限区间内，比如在估计噪
声过程的方差时（。≥0)，那么从数值计算角度来着，网格搜索法也许是不可行的。在这种情
况下，我们只好通过迭代来求最大值。经典的方法是Newton-Raphson方法、得分法和数学期望
最大算法。一般而言，如果设定的初始慎接近于真实最大值，那么这些方法可以求出MLE
否则迭代就不会收敛，或者只会收敛到局部最大值。使用这些选代方法的困难在于，常我们
事先并不知道它们是否收敛；或者即使收敛，所求得的值是否就是MLE。使得我们的估计问
题可与其他最大值求解区别开来的重要特性，则在于预先并不知道那个要求最大值的函数。
似然函数对于每个数据集是变化的，即要求的是·个随机函数的最大值。不过，这些方法有时
能得到好的结果。现在，我们来介绍些更通用的方法：［Bard1974]更为完整地介绍了应用
于估计问题的非线性最优化方法，感兴趣的读者可以参考
作为一个对比方法，我们会在下面的例题中应用这些方法。
p(z;)
- MLE
图7.8求解MLE的网格搜索法
例7.11WGN中的指数信号
考虑观测数据
c[n] = rn + w[n]
n= 0,1,-.-, N- 1
其中 w[n]是方差为 的WGN。需要进行估计的量是参数 ,即指数因子,要求r>0。r
的MLE就是使似然函数最大的厂值，
N-1
p(x;r) = 7
2α2
n=0
或者，可以等效为使下式最小的「值，
N-1
J(r) = Z (r[n] - r")2
0=t
对J(r)求导，并令它等于零,得
(7.25)
n=0
这是一个r的非线性方程，不能直接求解。我们将考虑采用Newton-Raphson 方法或得分
法的送代方法